process.env.DEBUG = 'index,AutelisHost'

const Config       = require('./config'),
      debug        = require('debug')('index'),
      AutelistHost = require('./lib/AutelisHost'),
      host         = new AutelistHost()


// function main() {
//     const host = new AutelistHost()

    // host.on('statechange', (newState, oldState) => {
    //     oldState = oldState || {}
    //     for (const key in newState) {
    //         if (oldState[key] === 'undefined' || oldState[key] !== newState[key]) {
    //             debug('publish', key, newState[key])
    //             client.publish(Config.mqtt.topic + '/' + key, String(newState[key]))
    //         }
    //     }
    // })
// }
//
// main()

